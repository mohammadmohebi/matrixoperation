To build the project go at the root of the project folder and execute theses commands.

```bash
mkdir build
cd build
cmake ..
make
```
`cmak` must be installed on the machine you are trying to build in.